[package]
name = "fem_poisson_1d"
version = "0.1.0"
edition = "2021"

# No dependencies on purpose. The exercise is about writing the algorithm, and a
# dependency-free crate also means `cargo run` works on any machine with a Rust
# toolchain and no network access.
[dependencies]

[profile.release]
opt-level = 3
